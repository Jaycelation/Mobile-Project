package com.example.kid_app.data;

/**
 * Package dữ liệu — Model và Repository.
 * Cấu trúc con:
 *   - model/      : Java POJO cho Firestore document
 *   - repository/ : Repository classes giao tiếp với Firestore
 *   - mapper/     : Helper chuyển đổi DocumentSnapshot <-> Model
 * Sẽ được triển khai ở Bước 2.
 */
public final class DataPackage {
    private DataPackage() {}
}
